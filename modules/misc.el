;; Turn off beep
(setq visible-bell t)

;; Disable visible scrollbar
(scroll-bar-mode -1)

;; Disable menu bar
(menu-bar-mode -1)

;; Disable toolbar
(tool-bar-mode -1)

;; Disable tooltips
(tooltip-mode -1)

;; Padding sides
(set-fringe-mode 20)
